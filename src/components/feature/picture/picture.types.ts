export type PictureBlockProps = {
  pictureBlockProps?: {
    className?: string;
  };
};

export type PictureImageProps = {
  pictureImageProps: {
    imageSource: string;
    imageAlt: string;
    className?: string;
  };
};
